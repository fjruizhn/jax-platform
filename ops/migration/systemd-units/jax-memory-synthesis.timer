[Unit]
Description=JAX — Worker de sintesis de segundo orden, una vez por dia (depende de facts verificados, ritmo humano, no de cada conversacion)

[Timer]
OnCalendar=*-*-* 04:30:00
Persistent=true

[Install]
WantedBy=timers.target
