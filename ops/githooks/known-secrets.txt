# Secretos YA CONOCIDOS -- lista de reintroduccion.
#
# Formato: <hmac-sha256-hex> <etiqueta>
#
# El valor NUNCA se versiona. Lo que va aca es HMAC-SHA256(pepper, valor),
# con el pepper en /etc/jax/precommit-pepper (0600), FUERA del repo.
#
# POR QUE HMAC Y NO SHA256 PELADO: este archivo viaja en un repo PUBLICO.
# Un sha256 de una contrasenia de 8 caracteres se crackea con wordlist en
# minutos -- publicarlo seria publicar la contrasenia de nuevo. Sin el
# pepper, estas lineas no dicen nada.
#
# La ETIQUETA no debe permitir reconstruir el valor: nombra la cuenta y la
# epoca, nunca la forma, el largo ni el charset.
#
# Agregar una entrada: ops/githooks/anadir-secreto.py (ver README.md).
401d1a5676a2da1be01c697eea273505cd3fb0f16d58f372124db5b9b3e08906  seed-superadmin-2026-06
