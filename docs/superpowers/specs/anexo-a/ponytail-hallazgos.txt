1. delete: 4 stale `*.backup-pre-T5-20260821-014420` copies inside src (en.js 364, es.js 367, PipelineModal.jsx 217, PipelineModal.test.jsx). They are untracked by git. Nothing replaces them. [frontend/src/i18n/en.js.backup-pre-T5-20260821-014420:L1-364, frontend/src/i18n/es.js.backup-pre-T5-20260821-014420:L1-367, frontend/src/components/BottomBar/PipelineModal.jsx.backup-pre-T5-20260821-014420:L1-217]
2. delete: the facet_models router module is no longer registered in main.py (main.py:L151 says so), and a 16-line comment explains keeping it. Nothing replaces it; git history keeps it. [backend/api/admin/facet_models.py:L1-124, backend/api/admin/__init__.py:L13-28]
3. shrink: the hand-rolled /etc/jax/.env parser is copied 4 times. One shared loader, or os.environ, which chat.py already populates. [backend/api/chat.py:L82-93, backend/api/image.py:L18-30, backend/api/admin/keys.py:L33-45, backend/api/admin/dashboard.py:L44-64]
4. shrink: `credential_resolver._db_conn` and `facet_resolver._db_conn` duplicate the guard and connect arguments of `db.connection.get_pool`. Use `get_pool()`. [backend/credential_resolver.py:L58-80, backend/facet_resolver.py:L229-251, backend/db/connection.py:L48-74]
5. shrink: env-number parsing is hand-rolled 4 times. One `env_positivo(var, default, conv, strict)` helper. [backend/uso/cola.py:L158-173, backend/uso/reintento.py:L138-149, backend/governance_context.py:L183-197, backend/db_connect_config.py:L81-103]
6. delete: helpers that only tests use live in production modules (`reset_intentos`, `intentos`, `reset_estado`, `reset_write_failures`, `_cache_clear`, `reset_login_limiters`). Move them to test fixtures. [backend/uso/reintento.py:L167-174, backend/uso/cola.py:L103-113, backend/facet_health.py:L59-63, backend/governance_context.py:L244-261, backend/auth/rate_limit.py:L154-156]
7. stdlib: `ChannelConnectionCounter` is a 27-line hand-built counter. `collections.Counter`. [backend/jax_engine/lifecycle.py:L15-41]
8. yagni: `resolve_credential_instrumented` keeps the DB-to-env fallback for a "7-day double-read window" that its own docstring says to retire. Call `resolve_credential` directly and drop `_PROVIDER_ENV_KEY_MAP`. [backend/credential_resolver.py:L29-38, L124-139]
9. yagni: 4 hand-rolled modal overlays (backdrop, panel, Escape). Use `<Dialogo>`, which already exists. [frontend/src/pages/admin/AdminMotors.jsx:L156-158, frontend/src/pages/admin/AdminFacetsModels.jsx:L204-206, L232-234, frontend/src/pages/admin/AdminSmtp.jsx:L225-234, frontend/src/components/BottomBar/PipelineModal.jsx:L202-206]
10. shrink: `_count_configured_keys` re-reads .env and keeps its own copy of the provider list (`_PROVIDERS_KEYS`). `sum(1 for p in keys.PROVIDERS if env.get(p["env_key"]))`. [backend/api/admin/dashboard.py:L13-19, L44-67]
11. delete: POST /api/facets/{facet}/status has no caller (the comment says so). Nothing replaces it. [backend/api/facets.py:L31-51]
12. shrink: `_contar_barato` repeats the scandir filter of `_listar`, and `_contar` adds a third layer. One `_pendiente(e)` predicate plus `sum(...)`. [backend/uso/cola.py:L240-299]
13. shrink: 24 `app.include_router(...)` lines in a row. Loop over a tuple of routers. [backend/main.py:L132-156]
14. yagni: `exigir_un_solo_proceso` is an import-time guard on argv and WEB_CONCURRENCY. The single-worker setting belongs in the systemd unit. [backend/auth/rate_limit.py:L56-75]
15. yagni: `KeyProvider` is an ABC with one implementation, `EnvKeyProvider`. `os.environ.get("FERNET_KEY","").encode()` inline in `_get_fernet`. [backend/crypto_secrets.py:L9-28, L41]
16. shrink: 12-line `_id_seguro`. `isinstance(s, str) and s not in ("", ".", "..") and s == os.path.basename(s)`. [backend/uso/cola.py:L178-189]
17. shrink: the BottomBar error message is built 3 times. One `addError(facet, detail)`. [frontend/src/components/BottomBar/BottomBar.jsx:L149-156, L207-214, L236-243]
18. yagni: `_CacheEntry` classes (with `__slots__` and `__init__`) only hold 2 or 3 fields. `NamedTuple`. [backend/credential_resolver.py:L47-52, backend/facet_resolver.py:L83-96]
19. shrink: the "resolve command" block (content, status, drop from pending) is repeated 3 times. One `_resolverComando(msgId, taskId, content)`. [frontend/src/store/useJaxStore.js:L327-332, L469-474, L486-491]
20. shrink: `create_access_token` and `create_refshresh_token` differ only in expiry and "type". One `_token(kind, seconds)`. [backend/auth/jwt.py:L18-39]
21. shrink: `asyncio.Lock`s wrap synchronous dict operations with no await inside (EventBus, WebSocketHub, ResourceManager). Drop the locks and the `async with` blocks. [backend/jax_engine/events.py:L13-30, backend/jax_engine/websocket_hub.py:L12-68, backend/jax_engine/resource_manager.py:L10-26]
22. shrink: `_MODEL_COLUMNS` string is split back into `_MODEL_FIELDS`. Define the tuple and use `", ".join(FIELDS)`. [backend/api/admin/models.py:L44-61]
23. delete: default labels in `getEyeState`; its one caller, HalEye, always passes them. Make them required parameters. [frontend/src/store/useJaxStore.js:L593-603, frontend/src/components/HalEye/HalEye.jsx:L23-27]
24. shrink: repository.py duplicates path validation in get_file and delete_file. One `_resolve(path)`. [backend/api/admin/repository.py:L50-56, L77-83]
25. yagni: `http_client.get_http_client` is `async` but never awaits, and `cabeceras_gemini` wraps a one-entry dict. Plain function, and inline `{"x-goog-api-key": key}`. [backend/http_client.py:L19-30]
26. shrink: `PLACEHOLDERS` is a map of lambdas that only forward to `t.*`. A 4-way ternary. [frontend/src/components/BottomBar/BottomBar.jsx:L67-75]
27. delete: `_user_tenant_map` is written but never read in production (only tests/test_sse_isolation.py:178). Nothing replaces it. [backend/jax_engine/state.py:L58, L77, L81]
28. delete: the `dry_run` branch and `mode` field of CommandRequest; the only caller, BottomBar.jsx:178, always sends "execute". Nothing replaces them. [backend/api/command.py:L27, L58, L102, L105-106]
29. delete: TokenPayload model, never referenced. Nothing replaces it. [backend/auth/models.py:L5-9]
30. delete: `send` returned by `createWebSocket` has no caller, and the `ws.onerror` handler is a no-op. Return `{ close }`. [frontend/src/api/websocket.js:L46-48, L78]
31. delete: `ResourceManager.active_count` is used only by tests. Nothing replaces it. [backend/jax_engine/resource_manager.py:L24-26]
32. yagni: `AISLAMIENTOS` accepts "REPEATABLE READ", but no caller passes it. Allow only READ COMMITTED. [backend/db/transaccion.py:L15, L26-28]
33. yagni: `user_id` parameter always receives 1. A constant. [backend/api/admin/keys.py:L58, L85, L98, L137-168]
34. yagni: `memo()` on components with no props (CenterPanel, BottomBar, Toast). Plain `export default function`. [frontend/src/components/CenterPanel/CenterPanel.jsx:L1, L34, frontend/src/components/BottomBar/BottomBar.jsx:L393, frontend/src/components/Notifications/Toast.jsx:L39]
35. yagni: the store's `_t()` locale lookup duplicates the I18nProvider language resolution. Export one `diccionarioActivo()` from i18n/index.jsx. [frontend/src/store/useJaxStore.js:L11-13, frontend/src/i18n/index.jsx:L18-20]
36. shrink: audit reader does strip, empty check, split("\n") and filter. `text.splitlines()[-20:]`. [backend/api/audit.py:L22-28]
37. shrink: `_update_history` trims history by hand. `deque(maxlen=MAX_TURNS*2)`. [backend/api/chat.py:L999-1008]
38. shrink: single-use locals `tenant_id`, `user_id` and `revised_prompt` (always `req.prompt`). Inline them. [backend/api/image.py:L94-97]
39. delete: legacy `localStorage.removeItem('jax_token'/'jax_user')` runs on every load. Nothing replaces it. [frontend/src/store/useJaxStore.js:L97-98]
40. delete: `useWebSocket` returns `wsRef.current`, and its one caller (Dashboard) ignores it. Drop the return. [frontend/src/store/useWebSocket.js:L13, L48, frontend/src/pages/Dashboard.jsx:L11]
41. delete: `useI18n`/`t` imported and called but never used. Remove both. [frontend/src/pages/Admin.jsx:L2, L14]
42. delete: unused fallbacks: `steps` can't be empty after the early return. `max_steps: steps.length, steps`. [frontend/src/components/BottomBar/PipelineModal.jsx:L181, L188-189]
43. delete: `SlidingWindowLimiter.__len__` has no caller. Nothing replaces it. [backend/auth/rate_limit.py:L102-103]
44. yagni: `_file_info(base)` always gets REPO_BASE, and `_safe_path`'s `filename=""` default is never used. Hardcode both. [backend/api/admin/repository.py:L14, L22, L43]
45. native: `@heroicons/react` is declared and never imported. Remove the dependency. [frontend/package.json:L15]
46. native: `psutil` is used for one `virtual_memory()` call. Read /proc/meminfo, or `os.sysconf('SC_PHYS_PAGES')`. [backend/api/admin/dashboard.py:L2, L115, backend/requirements.txt:L9]
47. native: axios is used by a single module. A fetch wrapper with `AbortSignal.timeout()` (the refresh interceptor would have to be rewritten). [frontend/src/api/client.js:L1, L47-113]
