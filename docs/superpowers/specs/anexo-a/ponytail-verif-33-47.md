33. PARTIAL — The claim is true. `user_id=1` is the only value ever passed (backend/api/admin/keys.py:137, 139, 168), and 1 is also written straight into the SQL at keys.py:~218 (`DELETE ... WHERE user_id = 1`). But turning it into a constant makes the fixed identity permanent: the key store stays tied to the seeded superadmin, not to the logged-in `user`. Tests replace `_get_db_key` with a stand-in that has the same `(pool, user_id, provider_id)` signature (tests/test_keys_http_pooling.py:31, test_t6_seguimiento.py:193), so removing the parameter breaks them. The most that is safe is one named constant for the scattered 1s.
34. HARMFUL — The components do take no props, but `memo` still does work here. `Dashboard` subscribes to the store through `useWebSocket()` (the token, user and several actions; frontend/src/store/useWebSocket.js:6-12). Every token refresh or user change re-renders `Dashboard`, and without `memo` that would re-render `CenterPanel` (the message list with markdown), `BottomBar` and `Toast`. `memo` was added on purpose in perf commit 66c6816 ("Zustand selectors + React.memo to cut unnecessary re-renders").
35. VALID — `_t()` (useJaxStore.js:11-13) and `I18nProvider` (i18n/index.jsx:17-19) both read `localStorage 'jax_lang'` and pick `en` or `es` on their own. One exported function returning the active dictionary is safe and changes no behavior.
36. PARTIAL — The code is verbose as described (api/audit.py:22-25). But `splitlines()[-20:]` drops the `if l.strip()` filter. Blank or whitespace-only lines would then count toward the last 20 and fail JSON parsing silently, so fewer than 20 events show. The remedy needs to keep the filter.
37. HARMFUL — api/chat.py:836-839 does `history = _conversations.get(user_id, [])` and then `semantic_context + history`. A list plus a deque raises `TypeError` in the chat path. The history is also typed and passed on as `list[dict]` (`recent_history`, chat.py:233, 1060), and tests/test_chat_conversation_eviction.py treats it as a list.
38. VALID — `tenant_id`, `user_id` and `revised_prompt` are each used once (api/image.py:94-104). Inlining is trivial and safe, as long as the comment "gpt-image-1 no incluye revised_prompt" is kept.
39. PARTIAL — True that nothing replaces it. It is a documented security purge from commit 3252d96 (2026-07-17), which moved the JWT and user data out of XSS-readable storage (comment at useJaxStore.js:95-96). Removing it leaves old token and user data behind in any browser not opened since July. The saving is two cheap lines, so this is a low-value removal with a small security cost.
40. VALID — The hook returns `wsRef.current` (useWebSocket.js:48), which is also stale (`null` on first render). Its only caller is `Dashboard.jsx:11`, which ignores the return value.
41. VALID — Admin.jsx:2 imports `useI18n` and line 14 destructures `t`, but `t` is never used in the file.
42. VALID — `getFacetOptions` returns a fixed list of 6 ids (PipelineModal.jsx:18-31). `selected` only ever holds those ids (lines 111, 148), and line 182 returns early when it is empty. So `buildSteps` always gives a non-empty array at line 183, and no test expects `steps: null` or `max_steps` 1.
43. FALSE — `__len__` has a caller: `assert len(lim) <= 100` in backend/tests/test_login_rate_limit.py:54. That test (`test_memoria_acotada_con_muchas_claves`) checks the memory cap on the limiter's keys (`max_keys`).
44. VALID — `_file_info` has one call site and it passes `REPO_BASE` (repository.py:43). Both `_safe_path` calls pass `parts[1]` (lines 54, 81), so the `filename=""` default is never used. `REPO_BASE` is already a module constant, so no new hardcoding is added.
45. VALID — `heroicons` appears only in frontend/package.json:15 and package-lock.json. There is no static or dynamic import anywhere in frontend/src, the vite config or index.html.
46. PARTIAL — True that there is one `psutil` call (api/admin/dashboard.py:115; requirements.txt:9; /home/fruiz/jax has no other use). But `os.sysconf('SC_PHYS_PAGES')` only gives total memory, not the used MB and percent the endpoint returns. Reading /proc/meminfo means hand-writing psutil's used/percent logic, the same kind of hand-rolled parser this audit criticizes elsewhere.
47. HARMFUL — Only client.js imports axios directly, but the axios instance and its error shape reach 48 files that import `api/client`. At least 13 non-test files read `err.response...`, including the shared `codigoDe()` (api/errores.js:7). The request option `{ timeout }` is used (useJaxStore.js:205), and tests load axios dynamically (client.test.js:113). A rewrite would also redo the most sensitive auth logic: the refresh, session-epoch, forced password change (U34) and password-change-in-flight (U27) interceptors.

Counts: VALID 7 (35, 38, 40, 41, 42, 44, 45) · PARTIAL 4 (33, 36, 39, 46) · FALSE 1 (43) · HARMFUL 3 (34, 37, 47)