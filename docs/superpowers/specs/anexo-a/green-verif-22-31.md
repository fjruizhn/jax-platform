22. HARMFUL — The claim that the sets match is true: an AST comparison shows all 10 sets and the tiebreak order are identical to `jax/jax/core/router.py`, and `check_mirror_sync.py` does not cover them. The proposed import breaks CI, though. `chat.py:105` adds `expanduser("~/jax")` to the path, which does not exist on the runner. CI clones jax to `/tmp/jax` and only sets `JAX_REPO_PATH`/`JAX_CONFIG_PATH` (`.github/workflows/policy.yml:604-612`), with no PYTHONPATH on the pytest step. The ficha says a missing `~/jax` already fails through `loaders`, but that is false: `loaders` reads `JAX_REPO_PATH` (`chat.py:634`). Every test that imports `main`/`chat` would fail to collect. The import also mixes up modules: `router.py:44` pulls in `jax.core.contrato_dispatch`, which runs `from facet_resolver import _db_conn` first (`contrato_dispatch.py:41-46`). Inside the backend that name resolves to jax-platform's own `backend/facet_resolver.py`. Today `chat.py` imports no `jax.core` module at all; the pattern is mirror copies plus a sync check. Only the alternative the ficha mentions (add the sets to `check_mirror_sync.py`) is safe.
23. VALID — `git grep ws_notifications` finds it only at `config_admin.py:18`; the i18n key `adminSettingsWsNotif` exists only in `es.js:489` and `en.js:486`. `AdminSettings.jsx` never shows it (its fields are at L70-101), and there are no tests or `~/jax` references. Deleting both keys keeps es/en in step, and the PUT (L96-114) accepts whatever keys are already stored.
24. PARTIAL — The alias map is real (`AdminDashboard.jsx:28-35`, 7 uses, only in that file). The remedy `tono="info"` means building the class at runtime (`text-${tono}`). Tailwind 3 cannot see classes built that way; they only work today because `text-info`, `text-aviso` and the others appear literally in other files. It would also blind the contrast guard in `src/tema/contraste.test.js:141-152`, which only matches literal `text-<token>`. Passing the full class (`tono="text-info"`) avoids both problems.
25. PARTIAL — The three-branch mapping is only duplicated in `users.py:421-436`. In `smtp.py`, `ValueError` is caught in a separate try around `construir_mensaje` (L182-189), and the send try (L190-206) only catches `UnicodeEncodeError` and `OSError`/`SMTPException`. A single helper would either start turning a `ValueError` from `enviar` into 503 (today it is a 500) or need split call sites. It also has to keep the context-specific log lines and the rule never to log `exc` for `UnicodeEncodeError`. The ficha's evidence misreads `smtp.py`.
26. VALID — Every browser path is same-origin. In dev, Vite proxies `/api` and `/ws` (`vite.config.js:17-20`). In production, nginx proxies both to :8080 (`ops/migration/nginx/jax-platform-frontend.conf`), and `client.js:48` uses `baseURL: '/api'`. The only direct call is the dev WebSocket to :8080 (`websocket.js:4-8`), and CORSMiddleware does not apply to WebSockets; nothing in `main.py` checks origins itself. Starlette does not reject simple requests from unlisted origins either. The `~/jax/loadtest` k6 scripts are not browsers. The change also removes hardcoded values, which the rules want.
27. VALID — `jwt.py:18-39`: the two payloads differ only in `exp` seconds and `type`. Callers (`api/auth.py:53,61,187`, `tests/identidades.py:93`) use the public wrappers, which the remedy keeps, and no test patches the `EXPIRE` constants.
28. PARTIAL — The claim is true (`audit.py:22-26`). A `deque` still reads the whole file line by line, blocking, inside an `async def`, so it cuts memory but not time or blocking (a rule-4 async issue that stays). The blank-line filter has to run before the `deque`, or fewer than 20 events come back, and the file needs a `with`. The ficha also says "tests: ninguno", but `tests/test_audit_ilegible.py` (5 tests) and `test_audit_superadmin.py` pin this behaviour, and the proposal passes them.
29. VALID — The only caller, `_ws_connect_and_subscribe` (`main.py:177`), runs after `websocket.accept()` at `main.py:209`, so the branch at `websocket_hub.py:15-16` never runs in production. All test fakes already set `application_state=CONNECTED` (`test_websocket_isolation.py:35`, `test_sse_isolation.py:39`, `test_admin_usuarios_guardas.py:168`), and no test checks that `accept` is called. The guard was added in 31d1772, as the ficha says.
30. VALID — The installed axios is 1.18.0. In a standard browser, `resolveConfig.js:74-80` calls `headers.setContentType(undefined)` for FormData, so the manual header at `BottomBar.jsx:82-84` does nothing.
31. PARTIAL — Removing the hardcoded hex values in `state.py:16-27,86` and `schemas.py:38` is justified, and the frontend throws `color` away (`useJaxStore.js:85-92`). But the `color` override in `/api/facets` comes from the DB column `facet.color_hex`, which the Bloque C comment (`facets.py:14-17`) names as the single source of truth. That is a documented decision the ficha's "porqué: ninguno" misses. The tema-tokens spec §11 (L569) also puts "changing facet identity colours in the DB or jax" out of scope. The report's own "No tocar" list marks `/api/facets` as needing Fernando's decision, so that part of the remedy needs his GO.

Counts: VALID 5 (23, 26, 27, 29, 30) · PARTIAL 4 (24, 25, 28, 31) · FALSE 0 · HARMFUL 1 (22)