1. PARTIAL: The main claim holds, and removing the line is safe. `aiosmtplib` appears only at `backend/requirements.txt:10` and in `ops/migration/MIGRATION.md:118`, and no code in either repo imports it. Two supporting claims are wrong. `smtp_config.py:28` doesn't exist: `smtplib` is imported at `backend/api/admin/smtp.py:8` and `users.py:3`. And jax CI does install this file: `/home/fruiz/jax/.github/workflows/policy.yml:943` runs `pip install -r /tmp/jax-platform/backend/requirements.txt`, in job `jacobs-gobernanza-db`. It is also installed in `backend/.venv` today (`aiosmtplib-5.1.2`).
2. VALID: `git grep heroicons` finds only `frontend/package.json:15`, and nothing under `frontend/src` or the frontend config imports it. `git log -S` returns only `5e28e9e`. The lock file (`package-lock.json:13,1028`) has to be regenerated in the same change, as the ficha says.
3. PARTIAL: The diagnosis is right. `pdfplumber` is neither in the venv nor in `requirements.txt`, so every PDF ends in the 422 at `backend/api/upload.py:52-70`, and the message is hardcoded Spanish. But the ficha's own label, `delete`, is the harmful option: a PDF would fall through to the latin-1 "text" branch, which the ficha admits. The explicit-415 alternative is incomplete. It does not remove `application/pdf` from `frontend/src/components/chat/AttachButton.jsx:6` or the `pdf` icon in `FileAttachment.jsx:5`. It also overlaps the report's own "No tocar" item that attachments never reach the model, which is a product decision for Fernando.
4. PARTIAL: The equivalence is true. `ALLOWED_TEXT_TYPES` has no reader, and every entry in `ALLOWED_IMAGE_TYPES` starts with `image/` (`upload.py:10-11,17`). But the redundancy hides the real defect: neither allowlist was ever enforced. Any client-supplied `image/*` passes, and everything else becomes "text". Deleting the constants erases the stated contract instead of enforcing it (fail-closed), and this upload chain is itself pending a decision.
5. VALID: `git grep TokenPayload` finds only the definition at `backend/auth/models.py:5`. There are 0 hits in `/home/fruiz/jax`, and `git log -S` shows only `5e28e9e` and `c9a0b82`.
6. VALID: Under systemd the loaders do nothing (`/etc/systemd/system/jax-platform.service:13` sets `EnvironmentFile=`, and the loader only uses `setdefault`). `conftest.py:22-23` loads the env before the app is imported. Removing them is actually safer than the ficha says. `main.py:14-15` decrypts the provider keys before the routers are imported, so on a manual run `chat.py:82-93` and `image.py:18-30` would later setdefault the still-encrypted values from disk. No floor or registry in `test_no_fail_open_except` depends on these sites (only `PISO_ARCHIVOS_ESCANEADOS=120` and the `chat.py` file being present).
7. PARTIAL: The parsers are identical (`dashboard.py:44-66` vs `keys.py:33-43`), but the remedy is a cosmetic fix on the wrong data source. The report itself says the real source is the `credential` table (`credential_resolver.py:83`). The fix also couples the dashboard to a private helper in `keys.py`, a module the report lists under "No tocar". "espejo: no" is also inexact: jax `policy/rules/P10-fail-open-prohibido.yaml:33` and `policy/generated/CORPUS.md:41` document `_count_configured_keys` by name, from the 2026-08-19 triage.
8. PARTIAL: The diagnosis is right. No `/health` route exists (only `/api/health` at `api/health.py:9`), `/` is served by `StaticFiles` (`main.py:329`), and `status_code<500` means "alive", so the card always shows alive. Three things are wrong:
   - The alternative "marcarla alive sin red" would hardcode a fabricated status, which breaks fail-closed and the rule against inventing data.
   - The test does not count two `_check_http` calls. `test_dashboard_http_pooling.py` asserts 0 client instantiations and needs no change.
   - The porqué commit is wrong: `git log -S'JAX Engine'` gives `5e28e9e`, not `153f93d`.
9. VALID: `jax_engine/state.py:29` defines `LAS_MANOS_URL` from the environment with the same default, and it is already used at `state.py:161` and `chat.py:34,868`. Replacing the literal at `dashboard.py:71` removes a hardcoded URL and behaves the same when the variable is unset.
10. PARTIAL: The SQL is right. The covering index `idx_axioma_usage_periodo(created_at, facet, model, request_type, …)` is at `migrations.py:1579-1580`, `created_at` is a TIMESTAMP, and the COALESCE and same-date caveats are correct. The ficha says "tests: ninguno", but `usage.py:281-287` records that EXPLAIN cannot tell the two forms apart on MariaDB ≥11.1. The precedent is a test that pins the WHERE text (`tests/test_uso_por_periodo.py`). Without that test, and with no load measurement, the change cannot be verified. The porqué commit is also wrong: `DATE(created_at) = %s` comes from `5e28e9e`.
11. VALID: `git grep recent_events` finds only `dashboard.py:133`. There are 0 readers in `frontend/` (including `recentEvents`) and 0 in `/home/fruiz/jax`. The key comes from `5e28e9e`.

Counts: VALID 5 (2, 5, 6, 9, 11) · PARTIAL 6 (1, 3, 4, 7, 8, 10) · FALSE 0 · HARMFUL 0.