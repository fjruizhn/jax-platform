12. VALID: `POST /api/admin/repo/save` and `SaveFileRequest` sit at `backend/api/admin/repository.py:88-108`. A grep for `repo/save|save_file|SaveFileRequest` across jax-platform finds only the definition. The frontend calls only `GET`/`DELETE /admin/repo/file` (`AdminRepository.jsx:52,64,73`). Grepping all of `~/jax` for `repo/save` or `admin/repo` finds only a mention of `AdminRepository.jsx` in `CONTEXT.md:357`, nothing that calls the endpoint. Nothing in `loadtest/*.js` uses it. `git log -S'/repo/save'` shows only 5e28e9e (v0.2) and c9a0b82 (chore). No documented decision keeps it. Removing it takes away an untested superadmin disk-write path.
13. VALID: `repository.py:65` maps everything that isn't `.png` to `image/jpeg`, and the list at L59 includes `.gif`, `.webp` and `.svg`. Running `mimetypes.guess_type` in the project venv (Python 3.14.4) returns png, jpeg (also for `.JPG`), gif, webp and svg+xml correctly, never None. The only consumers are `<img src>` (`AdminRepository.jsx:161`) and a download anchor (L75-81), so an SVG data URI does not run script.
14. VALID: the `send` at `websocket.js:78` has no reader. The only `.send(` calls in src are the internal auth at L22 and the fake WebSocket in `websocket.test.js:16`. Tests ignore the return value of `createWebSocket` (L33, L42). `Dashboard.jsx:11` calls `useWebSocket()` and discards the result. `wsRef` is still needed by the cleanup at `useWebSocket.js:44`.
15. VALID: in `Admin.jsx`, `t` appears only at L2 (import) and L14 (declaration). `Admin.test.jsx:13` only wraps the component in `I18nProvider` and still works. The child components call `useI18n` themselves.
16. VALID: for all 25 keys, `git grep -w` outside es.js/en.js finds nothing. The one exception is `adminKeyModelDeleteConfirmSum`, which appears only as a string fixture (`dialogosDelNavegador.test.js:73`) and in comments (`dialogosDelNavegador.js:9`, `.test.js:18`). I checked every computed access myself:
    - `t[\`adminBindingsCapability…\`]` and `t[\`adminModelsStatus…\`]/Source…`: prefixes that none of the 25 keys share.
    - `AdminSidebar` and `AdminFacetsModels` labelKeys: fixed lists of other keys.
    - `FOLDER_LABELS` (`adminRepo*`) and `REASON_KEY` (`adminProposalReason*`).
    - `avisoSesion`: only `sesion_invalida`/`sesion_expirada`.
    - `RightPanel` `clave`: `approveError`/`cancelError`.
    - `AdminSettings` `claveDeError`: limited to `CODIGOS_CONOCIDOS`.

    None of the 25 keys can come out of these. Each key appears exactly once in each language file. `AttachButton`/`FileAttachment` read `attachTooltip`/`attachUploading`/`attachReady`/`attachRemove`, not the four `attach*` keys to be deleted, so the pending attachments decision is not affected. Nothing in `~/jax` references them.
17. VALID: `_user_tenant_map` is written only at `state.py:58,77,81`. No reader exists, no dynamic `getattr`/`__dict__` access touches it, and `~/jax` has no mention. The only read is the assert in `test_sse_isolation.py:178`, and `connected_users` at L172 already covers it. One small gap: the test docstring at L156 also names the map and should be edited in the same change.
18. VALID: `_fire_completed` (`chat.py:1142-1153`, called at L1050 and L1112) builds the payload. The only reader is `useJaxStore.js:309-311`, which just sets `activeFacet: null` and never reads `content` or `message_preview`. `EventBus.publish` (`jax_engine/events.py:24-36`) only calls the subscriber callback and stores nothing, so there is no audit or history record. No backend tests reference it, and `~/jax` has no match. Keeping `facet` is harmless.
19. VALID: `image_generated` (`image.py:98-104`) and `command_started` (`command.py:47-53`) have no consumers in frontend/src, backend/tests or `~/jax` (the only frontend branch is `command_completed`). The bus doesn't persist anything, so no audit trail is lost. `schemas.py` is not a mirrored file (no match in `~/jax/scripts` or `.github`). Taking them out of the `EventType` Literal makes any future sender fail pydantic validation, which is fail-closed.
20. VALID: `useJaxStore.js:305-306` reads only `payload.pipeline_id` and builds the text with the i18n key `humanGateRequestedToast`. `test_pipeline_user_id.py:100` checks only the event_type. Removing `message` also removes a hardcoded Spanish string from `state.py:217`.
21. VALID: at `chat.py:902-912` both branches call `_call_ollama(…, history, message, config, f.model)` and differ only by `+ ident`. Adding `""` for other facets passes the same string. Tests mock or call `_call_ollama` directly and none check the structure of this branch. `test_policy_invoke_facet_envoltorio.py` covers `_invoke_facet`, not `_invoke_facet_dispatch`. The "Bug 3" comment has to stay. One caveat: the ficha places the lines at L901-L913, but the `if` actually starts at L902.

Counts: VALID 10 · PARTIAL 0 · FALSE 0 · HARMFUL 0