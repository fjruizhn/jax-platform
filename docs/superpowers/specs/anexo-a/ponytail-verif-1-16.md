1. VALID — The four `*.backup-pre-T5-20260821-014420` files are ignored by `.gitignore:27` (`*.backup-*`), not just untracked. The 4th is 116 lines. `git log --find-object` finds all four blobs byte-identical in commit `f17c38b`, so git already has them and deleting is safe.
2. VALID — Nothing imports `api/admin/facet_models.py`: grep over jax-platform backend/frontend and /home/fruiz/jax finds no import. Its routes (`/api/admin/facet-models/...`) have no frontend caller. The 16-line comment explains why the router was unregistered and says the module was kept only "sin borrar en esta corrida"; no decision says to keep it. The comments at `main.py:151` and `db/migrations.py:105` point to the file and need updating. The table stays, since `seed.py:82` and tests use it.
3. PARTIAL — The four copies are real. But `keys.py:48-55` (`_write_env_key`) rewrites the whole file from `_load_env()` (encrypted values on disk), so `os.environ` cannot replace it. The copies also differ: `setdefault` into environ vs a returned dict. One shared file loader is fine; the "or os.environ" option is wrong for keys.py.
4. HARMFUL — `_db_conn` is a mirrored symbol in two mirror groups, `credential_resolver` and `facet_resolver` (`/home/fruiz/jax/scripts/check_mirror_sync.py:174,253`, CI step `jax/.github/workflows/policy.yml:394`). Their copies in `jax/core` and `las_manos` have no `get_pool`. Swapping in `get_pool` fails jax CI with drift, puts the fail-closed credential path on the shared pool of 10, and breaks `tests/test_facet_resolver_db_guard.py`, which imports `_db_conn`.
5. PARTIAL — The duplication is real. But `cola.max_filas` is a mirrored `cola_uso` symbol (`check_mirror_sync.py:343`), and so is `db_connect_timeout_seconds` (mirror group `db_connect_config`). The copies also behave differently: cola and reintento fall back to the default on bad input (fail-soft), while `governance_context.py:188` and `db_connect_config.py:94` raise (fail-closed, and governance also rejects non-finite values). Only cola+reintento are a safe merge, and cola's side must be ported to `jax/core/cola_uso.py` at the same time.
6. PARTIAL — Tests are the only callers, confirmed by grep. Moving the helpers out is not clean: `reset_estado` is a mirrored `cola_uso` symbol, and the comment in `jax/scripts/check_mirror_sync.py` says `tests/test_cola_uso_escritores.py` requires the mirror list to cover the whole file. `reset_estado` also holds the stale-measurement rule (`cola.py:110-112`). `intentos()` is documented "para mirar en vivo". Fixtures would have to reach into private state. These are deliberate test hooks.
7. PARTIAL — `collections.Counter` is not a drop-in. `decrement` ignores unknown users and deletes an entry when it hits zero (`lifecycle.py:30-35`); a bare `Counter` would go negative and keep one zero entry per user forever (slow memory growth). Backing the class with a Counter still needs that logic, so little is saved.
8. HARMFUL — The docstring (`credential_resolver.py:125-129`) sets a measurable exit criterion: 7 days with no `source=env_fallback` log line plus at least one real key rotation. Nobody has verified it against production logs. `resolve_credential_instrumented` and `_PROVIDER_ENV_KEY_MAP` are mirrored (`check_mirror_sync.py:250,255`), with live callers in jax (`jacobs/executor.py`, `las_manos/motor_registry/worker.py`, `jax/muscles/base.py`). Removing the fallback early can fail-close facets that still need it.
9. VALID — There are hand-rolled overlays at every cited line, 5 in all (AdminFacetsModels has two). None has inert, focus return or ARIA except Smtp's partial one. That is exactly what `Dialogo.jsx:5-26` (Ruling U27) was built to fix. Two notes: backdrop-click-to-close in Smtp/PipelineModal goes away (intended, U24), and the revoke confirm in AdminFacetsModels is destructive, so it may belong in `ConfirmacionSuma`.
10. VALID — `_PROVIDERS_KEYS` (`dashboard.py:13-19`) equals the `env_key` values in `keys.PROVIDERS` (`keys.py:22-27`), and the parser copy matches `keys._load_env`, which has the same FileNotFoundError-only handling. Reusing `keys._load_env()` plus `keys.PROVIDERS` is safe. Out of scope: the real source of truth is now the DB `credential` table, not .env.
11. PARTIAL — It has no caller, true. But the comment at `facets.py:31-34` says it is kept on purpose: "queda como herramienta de administración". That decision is from commit `8552f4b` (T6-5b, 2026-09-15), which also restricted it to superadmin and added tests (`tests/test_t6_seguimiento.py:291-297`). Deleting it reverses a day-old owner decision, so it needs Fernando.
12. PARTIAL — The filter really is repeated. But `_contar` is not an extra layer: it is the fail-soft wrapper that returns the last known count (`cola.py:291-299`). `_contar_barato` sits on a hot path that was just load-measured (`cola.py:462-487`: 25 concurrent threads cost 230× one), and adding a Python function call per entry has to be re-measured. All three are mirrored `cola_uso` symbols, so jax must change in the same step.
13. VALID — `main.py:132-156` has 24 plain `include_router` calls, and a loop over a tuple is behavior-neutral. It is cosmetic, and the unregistration comment at L151 must be kept in the tuple.
14. HARMFUL — It is a deliberate fail-closed guard (dated 2026-09-12 at `rate_limit.py:13`, tested in `tests/test_login_residuos.py:64`). The login rate limit lives in one process's memory, so running several workers would quietly multiply it. The systemd unit sets the worker count; this guard is what checks it. Removing it gives up that check on a security limit.
15. HARMFUL — `_get_fernet` is a mirrored `crypto_secrets` symbol (`check_mirror_sync.py`), and `KeyProvider`/`EnvKeyProvider` exist identically in `jax/jax/core/crypto_secrets.py:19-37` and `las_manos/crypto_secrets.py`. Inlining in one repo breaks the jax CI mirror check. The seam is also a documented design point (B1.3, `docs/fase1-credenciales-diseno.md`) tied to open debt R2 (moving the key to a KMS).
16. PARTIAL — The one-liner behaves identically on Linux: I ran old and new side by side on 12 inputs (`a/b`, `/`, `a/`, `..`, `.`, empty, backslash, `None`, `5`, `../a` and more) and got the same results. But `_id_seguro` is a mirrored `cola_uso` symbol (`check_mirror_sync.py:360`; copy at `jax/jax/core/cola_uso.py:191`), and a jax-platform-only change turns jax CI red. It only works as a joint two-repo change.

Counts: VALID 5 (1, 2, 9, 10, 13) · PARTIAL 7 (3, 5, 6, 7, 11, 12, 16) · FALSE 0 · HARMFUL 4 (4, 8, 14, 15)