fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
python-jose[cryptography]>=3.3
bcrypt>=4.0
aiomysql==0.3.2
# PyMySQL es dependencia TRANSITIVA de aiomysql (aiomysql/__init__.py importa
# pymysql.converters.escape_dict a nivel de modulo). Fijada EXACTA a proposito:
# PyMySQL 1.2.1 (2026-09-17) quito escape_dict de pymysql.converters y rompe
# `import aiomysql` en cualquier instalacion nueva -- reventó el job
# jacobs-gobernanza-db de jax (run 35202422331). Mismo par de versiones que
# produccion (backend/.venv, verificado con `pip show`) y que jax. Ver
# tests/test_pymysql_escape_dict_pin.py: subir cualquiera de las dos requiere
# probar la combinacion antes, no es un bump de rutina.
PyMySQL==1.2.0
python-multipart>=0.0.9
psutil>=5.9
# Lo arrastraba uvicorn[standard] como dependencia transitiva y nadie lo
# declaraba. api/chat.py y shadow_validation.py importan policy/governance/
# del repo `jax`, que lee YAML: si un dia uvicorn deja de traerlo, el backend
# no arranca por una dependencia que nunca dijimos que necesitabamos.
PyYAML>=6.0
# Extracción de texto de PDF adjuntos (frente D, 2026-09-16). Versión FIJADA
# tras revisar licencia (BSD-3-Clause), soporte de Python 3.14 y CHANGELOG de
# seguridad en PyPI/GitHub -- ver el ledger del frente D. Reemplaza la rama
# `import pdfplumber`, que nunca estuvo instalada (todo PDF daba 422).
pypdf==6.19.0
