fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
python-jose[cryptography]>=3.3
bcrypt>=4.0
aiomysql>=0.2
python-multipart>=0.0.9
psutil>=5.9
aiosmtplib>=3.0
# Lo arrastraba uvicorn[standard] como dependencia transitiva y nadie lo
# declaraba. api/chat.py y shadow_validation.py importan policy/governance/
# del repo `jax`, que lee YAML: si un dia uvicorn deja de traerlo, el backend
# no arranca por una dependencia que nunca dijimos que necesitabamos.
PyYAML>=6.0
